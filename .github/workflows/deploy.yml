name: Deploy Full Static SPA to GitHub Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Node.js 22 (LTS)
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install All Dependencies
        run: npm install

      - name: Build Static Site with Nitro
        # The '|| true' ignores the irrelevant Phase 2 Node.js server crash
        run: npm run build || echo "Phase 2 server build failed, but Phase 1 static prerender succeeded! Proceeding..."

      - name: Setup Pages Infrastructure
        uses: actions/configure-pages@v5

      - name: Upload Finished Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Grabs the static files that Nitro successfully generated in Phase 1!
          path: './.output/public' 

      - name: Deploy Live Site
        id: deployment
        uses: actions/deploy-pages@v4